#!/bin/bash
#
# This is the command-line entry point on Mac.
#
# Launch the app directly to preserve argument boundaries (spaces/emoji/etc).
# Mac's `open` can split args, which breaks file paths.
"/Applications/Diffuse.app/Contents/MacOS/mac_launcher.sh" "$(pwd)" "$@"
